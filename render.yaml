services:
  - type: web
    name: social-arena
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run_ui.py
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /api/config
